from django.conf.urls import  url
from . import views

urlpatterns = [
    # Examples:
        # url(r'^escalations/analytics/get$', escalations.get_analytics, name='escalations_analytics'),
    # url(r'^$', 'Election_portal.views.home', name='home'),
    # url(r'^blog/', include('blog.urls')),


]
